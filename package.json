{
  "name": "simplish-lms",
  "version": "1.0.0",
  "main": "app.js",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "multer": "^2.2.0",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "jest": "^30.2.0"
  },
  "scripts": {
    "build": "npm run build --prefix frontend && node -e \"const fs = require('fs'); if (!fs.existsSync('tmp')) fs.mkdirSync('tmp'); fs.writeFileSync('tmp/restart.txt', String(Date.now()));\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "node app.js",
    "test:e2e": "npm run test:e2e --prefix frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}